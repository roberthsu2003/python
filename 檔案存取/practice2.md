# Python 課堂練習設計

## 今日教學重點

讀取 CSV 檔案(`csv.DictReader`、跳過說明列、篩選資料列、建立字典串列)

---

# 課堂範例回顧(學生剛才已練習)

## 原始程式

```python
import csv

nptc_data = []

# 讀取各鄉鎮市區人口密度 CSV 檔 (跳過第一行非欄位說明文字)
with open('各鄉鎮市區人口密度.csv', 'r', encoding='utf-8') as file:
    next(file)  # 跳過第一列說明
    
    dict_reader = csv.DictReader(file)
    for row in dict_reader:
        # 篩選「區域別」中含有「新北市」的資料列
        if '新北市' in row.get('區域別', ''):
            nptc_data.append({
                '統計年': row.get('統計年'),
                '區域別': row.get('區域別'),
                '年底人口數': row.get('年底人口數'),
                '土地面積': row.get('土地面積'),
                '人口密度': row.get('人口密度')
            })

print("新北市前兩筆行政區人口密度:")
for item in nptc_data[:2]:
    print(item)
```

## 使用的 Python 技術

- `import csv`
- `with open(...) as file:` 檔案存取語法
- `next(file)` 跳過檔案中第一列非欄位說明文字
- `csv.DictReader(file)` 將每一列讀成字典(欄位名稱為 key)
- `for row in dict_reader:` 逐列處理
- `row.get('欄位名稱', 預設值)` 取得欄位值
- `in` 判斷字串是否包含某關鍵字
- `list.append()` 建立字典串列
- 串列切片 `[:2]` 取前幾筆

## 學習目標

學生可以學會:

- 使用 `csv.DictReader` 讀取 CSV 檔案,並以欄位名稱取值
- 用 `next(file)` 跳過檔案中非資料列的說明文字
- 依條件篩選資料列,並整理成字典串列

---

# 本次練習題目(全新題目,與課堂範例不同)

> 說明:以下是針對相同教學重點設計的「新題目」,情境與資料和課堂範例不同,但用到的 Python 語法一致。

## 題目敘述

老師手上有一份全國空氣品質監測資料檔 `空氣品質監測.csv`,格式像這樣:

```
本資料為各測站每日空氣品質監測紀錄
監測日期,縣市,測站名稱,AQI,PM2.5,狀態
2026-06-01,台中市,豐原,68,22,普通
2026-06-01,台北市,古亭,45,15,良好
2026-06-01,台中市,忠明,72,28,普通
2026-06-02,高雄市,前鎮,95,45,對敏感族群不健康
2026-06-02,台中市,西屯,66,20,普通
```

檔案第一列是說明文字(不是欄位名稱),必須先跳過,第二列才是真正的欄位名稱。

請寫一支程式:

1. 讀取 `空氣品質監測.csv`
2. 跳過第一列說明文字
3. 使用 `csv.DictReader` 逐列讀取資料
4. 篩選出「縣市」欄位中含有「台中市」的資料列
5. 每一筆篩選出的資料,只保留「監測日期」「縣市」「測站名稱」「AQI」「PM2.5」五個欄位,存成字典並加入串列
6. 印出「台中市前兩筆空氣品質監測資料:」,並列印出前兩筆資料

## 與課堂範例的對應

- 沿用的語法/概念:`import csv`、`with open()`、`next(file)` 跳過說明列、`csv.DictReader`、`row.get()`、`in` 篩選關鍵字、字典串列、串列切片取前幾筆
- 換掉的部分:資料情境從「鄉鎮市區人口密度」改成「空氣品質監測資料」,篩選關鍵字從「新北市」改成「台中市」,欄位名稱也不同

---

# 學生練習:半成品

以下兩種版本皆針對「本次練習題目」,教師可依學生程度選用。

## 版本 A:程式碼半成品(挖空補完)

適合剛接觸本主題的學生:程式架構已給,補完標示處即可。

### 任務說明

請完成以下程式中標示 `# 請完成` 的部分。

```python
import csv

air_data = []

# 讀取空氣品質監測 CSV 檔 (跳過第一行非欄位說明文字)
with open('空氣品質監測.csv', 'r', encoding='utf-8') as file:
    # 請完成:跳過第一列說明文字

    dict_reader = csv.DictReader(file)
    for row in dict_reader:
        # 請完成:篩選「縣市」欄位中含有「台中市」的資料列
        if # 請完成:
            air_data.append({
                '監測日期': row.get('監測日期'),
                # 請完成:補上其餘四個欄位（縣市、測站名稱、AQI、PM2.5）
            })

print("台中市前兩筆空氣品質監測資料:")
# 請完成:印出 air_data 的前兩筆資料
```

學生需要完成:

1. 用 `next(file)` 跳過第一列說明文字
2. 寫出判斷「縣市」欄位是否含有「台中市」的條件式
3. 補齊字典中其餘四個欄位,以及用切片印出前兩筆資料

### 完成後參考程式碼(教師用,勿發給學生)

> 此區塊僅供教師參考核對,是版本 A 補完後的完整正確程式。

```python
import csv

air_data = []

# 讀取空氣品質監測 CSV 檔 (跳過第一行非欄位說明文字)
with open('空氣品質監測.csv', 'r', encoding='utf-8') as file:
    next(file)  # 跳過第一列說明

    dict_reader = csv.DictReader(file)
    for row in dict_reader:
        # 篩選「縣市」欄位中含有「台中市」的資料列
        if '台中市' in row.get('縣市', ''):
            air_data.append({
                '監測日期': row.get('監測日期'),
                '縣市': row.get('縣市'),
                '測站名稱': row.get('測站名稱'),
                'AQI': row.get('AQI'),
                'PM2.5': row.get('PM2.5')
            })

print("台中市前兩筆空氣品質監測資料:")
for item in air_data[:2]:
    print(item)
```

## 版本 B:純註解半成品(依註解寫程式)

適合已熟悉本主題、要訓練獨立寫程式的學生:只有註解,沒有程式碼,請依註解逐行寫出完整程式。

### 任務說明

請依照下方每一行註解的說明,寫出對應的 Python 程式碼。

```python
# 步驟 1:匯入 csv 模組


# 步驟 2:建立一個空串列 air_data，用來存放篩選後的資料


# 步驟 3:使用 with open() 開啟 空氣品質監測.csv（讀取模式，編碼 utf-8）


    # 步驟 4:用 next(file) 跳過第一列的說明文字


    # 步驟 5:用 csv.DictReader 建立可以逐列讀取欄位資料的物件


    # 步驟 6:用 for 迴圈逐列讀取資料


        # 步驟 7:判斷這一列的「縣市」欄位是否含有「台中市」


            # 步驟 8:如果符合條件，把「監測日期」「縣市」「測站名稱」「AQI」「PM2.5」
            #        整理成字典，加入 air_data


# 步驟 9:印出「台中市前兩筆空氣品質監測資料:」


# 步驟 10:用 for 迴圈搭配串列切片，印出 air_data 的前兩筆資料

```

學生需要依序完成每個步驟的程式碼。

### 完成後參考程式碼(教師用,勿發給學生)

> 此區塊僅供教師參考核對,是依版本 B 註解逐步寫出的完整正確程式。

```python
# 步驟 1:匯入 csv 模組
import csv

# 步驟 2:建立一個空串列 air_data，用來存放篩選後的資料
air_data = []

# 步驟 3:使用 with open() 開啟 空氣品質監測.csv（讀取模式，編碼 utf-8）
with open('空氣品質監測.csv', 'r', encoding='utf-8') as file:
    # 步驟 4:用 next(file) 跳過第一列的說明文字
    next(file)

    # 步驟 5:用 csv.DictReader 建立可以逐列讀取欄位資料的物件
    dict_reader = csv.DictReader(file)

    # 步驟 6:用 for 迴圈逐列讀取資料
    for row in dict_reader:
        # 步驟 7:判斷這一列的「縣市」欄位是否含有「台中市」
        if '台中市' in row.get('縣市', ''):
            # 步驟 8:如果符合條件，把「監測日期」「縣市」「測站名稱」「AQI」「PM2.5」
            #        整理成字典，加入 air_data
            air_data.append({
                '監測日期': row.get('監測日期'),
                '縣市': row.get('縣市'),
                '測站名稱': row.get('測站名稱'),
                'AQI': row.get('AQI'),
                'PM2.5': row.get('PM2.5')
            })

# 步驟 9:印出「台中市前兩筆空氣品質監測資料:」
print("台中市前兩筆空氣品質監測資料:")

# 步驟 10:用 for 迴圈搭配串列切片，印出 air_data 的前兩筆資料
for item in air_data[:2]:
    print(item)
```

---

# 延伸挑戰題

## 題目

請修改程式,計算並印出「台中市」所有測站的 **PM2.5 平均值**(四捨五入到小數點第 1 位),並額外印出 PM2.5 數值最高的那一筆資料(包含測站名稱與數值)。

## 提示

可以思考:

- CSV 讀進來的數值(例如 PM2.5)是字串還是數字?要不要先用 `float()` 或 `int()` 轉換,才能計算平均或比大小?
- 如何在一個串列裡,找出某個欄位數值最大的那一筆資料?(可以想想 `max()` 搭配 `key` 參數)
- 平均值要四捨五入,可以用哪個函式?

---

# 思考問題

1. 如果拿掉 `next(file)` 這一行,直接用 `csv.DictReader` 讀取檔案,會發生什麼問題?為什麼?
2. `row.get('縣市', '')` 為什麼要加上第二個參數 `''` 當作預設值?如果某一列剛好沒有「縣市」這個欄位,不加預設值會發生什麼事?
3. 如果要篩選「縣市」欄位「完全等於」台中市(而不是「包含」台中市這幾個字),程式該怎麼修改?這樣寫和原本用 `in` 有什麼差別?

---

# 建議查詢方向

學生可以搜尋:

1. Python `csv.DictReader` 用法與範例
2. Python 字典 `get()` 方法與預設值的用途
3. Python `round()` 四捨五入用法,以及 `max()` 搭配 `key` 參數找出最大值

