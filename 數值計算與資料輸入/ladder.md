# Python 課堂練習設計

## 今日教學重點

變數(variable)與數學運算子

---

# 範例分析

## 原始程式

```python
#!usr/bin/python3
'''
讓使用者輸入梯形的上底、下底及高，程式會計算梯形的面積(上底加下底乘以高除以2)
'''

top = float(input('請輸入梯形的上底(公分):'))
bottom = float(input('請輸入梯形的下底(公分):'))
height = float(input('請輸入梯形的高(公分):'))
area = (top + bottom) * height / 2
print('梯形的面積:', area, '平方公分')
```

## 使用的 Python 技術

- input()
- float()
- print()
- variable(變數)
- 數學運算子(+、*、/)
- 運算優先順序與括號

## 學習目標

學生可以學會:

- 用 `input()` 取得使用者輸入,並用 `float()` 轉成數字
- 把計算過程的結果存進變數
- 用括號控制數學運算的先後順序

---

# 學生練習:半成品

## 任務說明

請完成以下程式。

```python
#!usr/bin/python3
'''
讓使用者輸入梯形的上底、下底及高，程式會計算梯形的面積(上底加下底乘以高除以2)
'''

# 步驟 1:請取得上底、下底、高,記得轉成數字
top = float(input('請輸入梯形的上底(公分):'))
bottom = ___________________________________   # 請完成:取得下底
height = ___________________________________   # 請完成:取得高

# 步驟 2:請寫出梯形面積公式 (上底 + 下底) × 高 ÷ 2
area = _____________________________________   # 請完成:計算面積

# 步驟 3:把結果印出來
print('梯形的面積:', area, '平方公分')
```

學生需要完成:

1. 仿照上底的寫法,取得下底與高,並轉成數字
2. 正確使用括號寫出面積公式
3. 確認輸出結果正確

---

# 延伸挑戰題

## 題目

請寫一支程式,讓使用者輸入「圓形的半徑」,計算並印出這個圓的面積與周長。
(圓面積 = 半徑 × 半徑 × 圓周率;圓周長 = 2 × 圓周率 × 半徑,圓周率請用 3.14159)

## 提示

可以思考:

- 圓周率是一個固定的數字,要不要也存成一個變數?
- 半徑要乘自己兩次,可以怎麼寫?
- 面積和周長是兩個不同的計算結果,需要幾個變數來存?

---

# 思考問題

1. 如果把面積公式的括號拿掉,寫成 `top + bottom * height / 2`,計算結果會不會一樣?為什麼?
2. 程式裡為什麼要用 `float()` 把輸入包起來?如果不用會發生什麼事?
3. 如果想讓答案只顯示到小數點後兩位,你會往哪個方向去查資料?

---

# 建議查詢方向

學生可以搜尋:

1. 搜尋「Python input float 將輸入轉換成數字」
2. 搜尋「Python 數學運算子 運算優先順序 括號」
3. 搜尋「Python print 數字 四捨五入 小數點位數」

---

# 教師備課備註

本練習主要訓練:

- 變數的指定與使用
- 數學運算子與括號控制運算順序
- input() 搭配 float() 取得數值輸入

避免加入:

- if 判斷、迴圈等尚未教學的內容
- 函式(function)或匯入套件(如 math)的寫法

要我把這份練習存成 Markdown 或 Word 檔給學生用嗎?