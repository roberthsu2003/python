# Python 課堂練習設計

## 今日教學重點

巢狀表格結構(List of Dicts)

---

# 課堂範例回顧(學生剛才已練習)

## 原始程式

```python
# 1. 宣告 list[dict] 的巢狀表格結構
students = [
    {"name": "愛麗絲", "國文": 85, "數學": 92, "英文": 78},
    {"name": "鮑伯", "國文": 79, "數學": 85, "英文": 88},
    {"name": "卡蘿", "國文": 92, "數學": 88, "英文": 95}
]

# 2. 列印表格表頭 (使用 f-string 對齊)
# 注意：姓名欄靠左對齊，成績與平均值靠右對齊
print(f"{'姓名':<5} {'國文':>4} {'數學':>4} {'英文':>4} {'平均成績':>5}")
print("-" * 40)

# 3. 遍歷每筆學生記錄並格式化輸出
for student in students:
    name = student["name"]
    chinese = student["國文"]
    math = student["數學"]
    english = student["英文"]
    
    # 計算平均分數
    avg = (chinese + math + english) / 3
    
    # 輸出對齊列
    print(f"{name:<6} {chinese:>6d} {math:>6d} {english:>6d} {avg:>8.1f}")
```

## 使用的 Python 技術

- list of dict(巢狀表格結構)
- for 迴圈遍歷 list
- dict 的 key 取值(`student["key"]`)
- f-string 格式化與對齊(`:<`、`:>`、`.1f`)
- 迴圈內累加運算

## 學習目標

學生可以學會:

- 理解「list 包 dict」的巢狀結構如何用來儲存一份表格資料(多筆記錄、每筆記錄有多個欄位)
- 用 for 迴圈遍歷 list,並用 `student["key"]` 的方式取出每一筆 dict 中的欄位值
- 用 f-string 的 `:<`、`:>` 對齊語法,搭配計算出的新欄位(如小計),印出一張排列整齊的表格

---

# 本次練習題目(全新題目,與課堂範例不同)

> 說明:以下是針對相同教學重點設計的「新題目」,情境與資料和課堂範例不同,但用到的 Python 語法一致。

## 題目敘述

小七便利商店店長想請店員列印一張「本日熱銷商品彙整表」。店長提供了每項商品的名稱、單價與銷售數量,請寫程式:

1. 用 list of dict 儲存每項商品的 `name`(商品名稱)、`price`(單價)、`qty`(銷售數量)
2. 印出表格表頭(商品名稱靠左對齊,單價、數量、小計靠右對齊)
3. 遍歷每一筆商品資料,計算「小計」= 單價 × 數量
4. 用 f-string 對齊格式,印出整齊的商品明細表

## 與課堂範例的對應

- 沿用的語法/概念:list of dict 巢狀結構、for 迴圈遍歷、`dict["key"]` 取值、f-string `:<`/`:>` 對齊格式化、迴圈內計算新欄位
- 換掉的部分:情境從「學生成績表」換成「商店商品銷售表」;計算欄位從「三科平均」換成「單價 × 數量的小計」,且小計為整數而非浮點數(對齊格式需相應調整)

---

# 學生練習:半成品

以下兩種版本皆針對「本次練習題目」,教師可依學生程度選用。

## 版本 A:程式碼半成品(挖空補完)

適合剛接觸本主題的學生:程式架構已給,補完標示處即可。

### 任務說明

請完成以下程式中標示 `# 請完成` 的部分。

```python
# 1. 宣告 list[dict] 的商品銷售資料
products = [
    {"name": "御飯糰", "price": 30, "qty": 12},
    {"name": "茶葉蛋", "price": 15, "qty": 20},
    {"name": "運動飲料", "price": 25, "qty": 8}
]

# 2. 列印表格表頭 (使用 f-string 對齊)
# 注意：商品名稱靠左對齊，單價、數量、小計靠右對齊
print(f"{'商品名稱':<6} {'單價':>4} {'數量':>4} {'小計':>6}")
print("-" * 30)

# 3. 遍歷每筆商品記錄並格式化輸出
for product in products:
    name = product["name"]
    price = # 請完成:從 product 這個 dict 中取出 "price" 欄位的值
    qty = # 請完成:從 product 這個 dict 中取出 "qty" 欄位的值

    # 計算小計金額 (單價 x 數量)
    subtotal = # 請完成:計算 price 乘以 qty

    # 輸出對齊列
    print(f"{name:<6} {price:>6d} {qty:>6d} {subtotal:>8d}")
```

學生需要完成:

1. 取出 `product` 這個 dict 中的 `"price"` 欄位值,存入 `price`
2. 取出 `product` 這個 dict 中的 `"qty"` 欄位值,存入 `qty`
3. 計算 `subtotal`(小計 = 單價 × 數量)

### 完成後參考程式碼(教師用,勿發給學生)

> 此區塊僅供教師參考核對,是版本 A 補完後的完整正確程式。

```python
# 1. 宣告 list[dict] 的商品銷售資料
products = [
    {"name": "御飯糰", "price": 30, "qty": 12},
    {"name": "茶葉蛋", "price": 15, "qty": 20},
    {"name": "運動飲料", "price": 25, "qty": 8}
]

# 2. 列印表格表頭 (使用 f-string 對齊)
# 注意：商品名稱靠左對齊，單價、數量、小計靠右對齊
print(f"{'商品名稱':<6} {'單價':>4} {'數量':>4} {'小計':>6}")
print("-" * 30)

# 3. 遍歷每筆商品記錄並格式化輸出
for product in products:
    name = product["name"]
    price = product["price"]
    qty = product["qty"]

    # 計算小計金額 (單價 x 數量)
    subtotal = price * qty

    # 輸出對齊列
    print(f"{name:<6} {price:>6d} {qty:>6d} {subtotal:>8d}")
```

## 版本 B:純註解半成品(依註解寫程式)

適合已熟悉本主題、要訓練獨立寫程式的學生:只有註解,沒有程式碼,請依註解逐行寫出完整程式。

### 任務說明

請依照下方每一行註解的說明,寫出對應的 Python 程式碼。

```python
# 步驟 1:建立 products 這個 list,裡面放入 3 筆 dict,每筆包含 name(商品名稱)、price(單價)、qty(數量)


# 步驟 2:印出表頭,商品名稱靠左對齊、單價/數量/小計靠右對齊


# 步驟 3:印出一條分隔線(例如 30 個 "-")


# 步驟 4:用 for 迴圈遍歷 products 中的每一筆商品資料

    # 步驟 5:從目前這筆商品中取出 name

    # 步驟 6:從目前這筆商品中取出 price

    # 步驟 7:從目前這筆商品中取出 qty

    # 步驟 8:計算小計 subtotal = price 乘以 qty

    # 步驟 9:用 f-string 對齊格式,印出這一列商品明細(名稱、單價、數量、小計)
```

學生需要依序完成每個步驟的程式碼。

### 完成後參考程式碼(教師用,勿發給學生)

> 此區塊僅供教師參考核對,是依版本 B 註解逐步寫出的完整正確程式。

```python
# 步驟 1:建立 products 這個 list,裡面放入 3 筆 dict,每筆包含 name(商品名稱)、price(單價)、qty(數量)
products = [
    {"name": "御飯糰", "price": 30, "qty": 12},
    {"name": "茶葉蛋", "price": 15, "qty": 20},
    {"name": "運動飲料", "price": 25, "qty": 8}
]

# 步驟 2:印出表頭,商品名稱靠左對齊、單價/數量/小計靠右對齊
print(f"{'商品名稱':<6} {'單價':>4} {'數量':>4} {'小計':>6}")

# 步驟 3:印出一條分隔線(例如 30 個 "-")
print("-" * 30)

# 步驟 4:用 for 迴圈遍歷 products 中的每一筆商品資料
for product in products:

    # 步驟 5:從目前這筆商品中取出 name
    name = product["name"]

    # 步驟 6:從目前這筆商品中取出 price
    price = product["price"]

    # 步驟 7:從目前這筆商品中取出 qty
    qty = product["qty"]

    # 步驟 8:計算小計 subtotal = price 乘以 qty
    subtotal = price * qty

    # 步驟 9:用 f-string 對齊格式,印出這一列商品明細(名稱、單價、數量、小計)
    print(f"{name:<6} {price:>6d} {qty:>6d} {subtotal:>8d}")
```

---

# 延伸挑戰題

## 題目

店長希望在表格最下方,再多印出一列「合計」,顯示所有商品的「小計總和」(也就是今日總營業額),並且合計金額也要跟上方的小計欄位對齊。

## 提示

可以思考:

- 在 for 迴圈開始之前,先宣告一個變數 `total = 0` 當作累加器
- 每次算出 `subtotal` 之後,用 `total += subtotal` 把它累加進去
- 迴圈跑完之後,在表格最下方再印出一條分隔線,以及一列「合計」文字搭配 `total`,並使用與商品明細相同的對齊寬度

## 提示補充

- `total` 是浮點數還是整數?這會影響你在 f-string 裡要用 `:>8d` 還是要調整格式
- 也可以想想:如果店長只想看「小計超過 300 元」的商品,要在迴圈裡加上什麼條件判斷?

---

# 思考問題

1. 為什麼要把每一項商品的資料放進一個 dict,而不是分別用三個獨立的 list(一個放名稱、一個放單價、一個放數量)?用巢狀結構有什麼好處?
2. 如果把 `print(f"{name:<6} ...")` 中的 `<6` 改成 `>6`,表格的排列會有什麼變化?為什麼靠左對齊比較適合文字欄位,靠右對齊比較適合數字欄位?
3. 如果商品資料多達 100 筆,你覺得目前的寫法(for 迴圈逐筆計算並印出)可以應付嗎?如果要額外計算「銷售總額」,程式需要做什麼調整?

---

# 建議查詢方向

學生可以搜尋:

1. Python f-string 格式化語法 `:<` `:>` 對齊用法
2. Python list 裡面放 dict 的巢狀資料結構寫法
3. Python 迴圈中如何使用累加器(accumulator)計算總和

